function clickToChange () {
    var h1 = document.getElementById("clickH1")
    h1.innerText = (newTitle)
}


let newTitle = ["Title", "Hejsan", "Kalas"]

function randomTitle(newTitle) {
    for (var i=0; i < newTitle.length; i++) {
      result += newTitle(Math.floor(Math.random))  
    }    
}